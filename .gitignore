#env
.env
.env.*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.vercel

# Python bytecode caches
__pycache__/
*.pyc
*.pyo
.env*.local

# Claude Code local config + worktrees (per-machine state)
.claude/
knowledge/.claude/

# Supabase CLI version cache + other transient state
supabase/.temp/

# Personal docs / sensitive paperwork dropped at the repo root
# (credit apps, signed agreements, etc). Keep them on disk but never
# commit them.
/*.pdf

# Local scratch notes
CONTEXT.md

# Playwright outputs — never commit
playwright-report/
test-results/
/blob-report/
/playwright/.cache/

# Local output of scripts/backup-storage.mjs (OPS-02)
storage-backup/

# Capacitor / iOS native build artifacts
/ios/App/Pods/
/ios/App/App/public/
/ios/App/build/
/ios/App/DerivedData/
*.xcuserstate
