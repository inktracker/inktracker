{
  "rewrites": [
    {
      "_comment": "SPA fallback only for routes without a file extension. Paths containing a dot (e.g. /landing/quote-demo/index.html, /icon-192.png) are served as static files instead of being rewritten to the React app.",
      "source": "/((?!.*\\..*).*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Frame-Options", "value": "ALLOWALL" },
        { "key": "Content-Security-Policy", "value": "frame-ancestors *" }
      ]
    }
  ]
}
